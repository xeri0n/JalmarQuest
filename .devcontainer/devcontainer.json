{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  // Removed the `android-sdk` feature because the runner reported permission/access
  // errors when fetching `ghcr.io/devcontainers/features/android-sdk:1`.
  // Re-enable or change this block if you have access to the feature registry.
  "features": {}
}